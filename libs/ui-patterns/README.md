# @dldc/ui-patterns

> Pre-composed UI components and interaction patterns built on top of `@dldc/ui-components` and `@dldc/ui-ariakit`. Patterns provide ready-made solutions for common use cases (such as a fully-formed Dialog with title, close button, and backdrop) while remaining flexible for customization.
