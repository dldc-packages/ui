# @dldc/ui-core

> The core design tokens and CSS variables for the DLDC design system.

This core package expose basic colors, spacing, typography design tokens as CSS variables as well as some helpers.
