{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "outputLogs": "new-only"
    },
    "typecheck": {
      "outputs": [],
      "dependsOn": ["^build"],
      "outputLogs": "new-only"
    }
  }
}
